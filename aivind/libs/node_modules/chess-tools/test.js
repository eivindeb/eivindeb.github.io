const fs = require("fs");

const ChessTools = require('chess-tools');
const OpeningBook = ChessTools.OpeningBooks.Polyglot;
const book = new OpeningBook();
const fen = "rnbqk2r/ppppppbp/5np1/8/2P5/2N3P1/PP1PPP1P/R1BQKBNR w KQkq";
var stream = fs.createReadStream("sample-data/gm2001.bin");
//console.log(stream);
book.load_book(stream);
book.on("loaded", ()=> {
        let entries = book.find(fen);
        console.log(entries)
        for (let entry of entries) {
            //See entry.js for each module to manage data.
        }
    });
//console.log(book);
